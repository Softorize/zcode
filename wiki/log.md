# Log

Append-only chronological record of every ingest and structural change. Newest at the top.

Format: `YYYY-MM-DD | source | pages touched | one-line summary`

---

2026-05-31 | Phase 22 Task 22.5 (agent-loop-deep-14) | pages/reactive-compaction-already-wired.md (new), pages/fallback-swap-already-wired.md, index.md | Audit over-reported again: the 413 -> reduce-history -> retry recovery was already wired+tested in agent_history.callWithAdapter (built as Task 7.5, MAX_REACTIVE_RETRIES=2 spiral guard, borrow-vs-own handled). Only real gap closed: surface the recovery onto TurnResult.compaction_applied via a new reactive_applied_out out-param (mirrors fallback_out), folded into compaction_applied_any in the turn loop.
2026-05-30 | Phase 15 Task 6 (ui-render-07) | pages/decisions/syntax-highlight-diff-gate.md (new), index.md | Added `CLAUDE_CODE_SYNTAX_HIGHLIGHT` env gate around diff-content highlighting. zcode defaults ON (reference defaults OFF) since it has always highlighted context+solo diff lines; only a falsy value (0/false/no/off) turns it off. Flag read once and threaded as `syntax_on` to `writeDiffCodeLine`; new `writeCodeContent` helper. Test footgun: keyword SGR is isatty-gated, so tests assert the OFF path hard.
2026-05-23 | KAIROS phase 1 implementation | pages/gotchas/test-discovery.md (new), index.md | `zig build test` silently skips tests of modules only reached via function-body imports; must register in src/main.zig comptime block. Surfaced while wiring cron/kairos modules.
2026-05-23 | Skill parity (PRD #532) | pages/gotchas/inferred-error-set-cycles.md (new), index.md | Adding an AgentRuntime method that loops back into handlePrompt triggers `dependency loop` build errors; fix by annotating one function `anyerror`. Surfaced while wiring context:fork skill execution.
2026-05-23 | CC parity (PRD #534) | pages/decisions/cc-parity-deep-modules.md (new), index.md | 9 phases of functional-equivalence parity (0.11.53-0.11.61); 19 new pure deep modules. Key lesson: the exploration-agent gap inventory over-reported missing features 6+ times - verify before building. Removal step deferred for user veto.
2026-05-23 | Prompt parity (PRD #533) | pages/gotchas/anthropic-prompt-caching.md (new), index.md | Anthropic caching is prefix-based (system->tools->messages); caching the whole system busts every turn. Cache the static prefix only by splitting at __SYSTEM_PROMPT_STATIC_BOUNDARY__. Surfaced implementing P3.

2026-05-25 | Native question-stall fix (0.11.72) | pages/gotchas/native-mode-steering-heuristics.md (new), index.md | Live REPL deadlock: Sonnet 4-6 replied "Before diving in, I need to clarify a few things to build this right." then ended the turn with no questions for the user to answer. PRD #533 dropped all native nudges; 0.11.70 re-added action-narration ones; 0.11.72 re-adds question-narration ones. Page documents the canonical set and the recipe for adding new stall patterns.
2026-05-26 | Question-stall hardening (0.11.73) | pages/gotchas/native-mode-steering-heuristics.md (updated), index.md (no change) | 0.11.72's single-attempt-shared-cap nudge fired correctly but Sonnet 4-6 just rephrased the announcement 4 times (128k tokens, no progress, two responses byte-identical). Followup adds three guards: separate cap of 2, escalating nudge, identical-response break. Lesson promoted to the wiki page recipe.
2026-05-30 | Phase 12 Task 19 (direct-connect server) | pages/gotchas/test-discovery.md (updated) | A top-level `const x = @import("file.zig")` that is used at runtime still does NOT get its tests discovered; remote_daemon.zig had 6 dormant tests until added to the comptime block. Sharper instance of the existing function-body-import gotcha.
2026-05-29 | P5 lifecycle hook EMISSION wiring (PRD #534) | pages/gotchas/lifecycle-hook-emission-wiring.md (new), index.md | The hook ENGINE (core/hooks.zig runEvent + full stdout contract) was already built and tested, but NOTHING in the live turn loop called it, so non-tool lifecycle hooks never fired in the running app. Wired the emission call sites in agent_runtime.zig: SessionStart (lazy on first turn -- init returns a value not *Self), UserPromptSubmit (blocks pre-prompt via early TurnResult), Stop (force-continue via labeled `stop_retry: while(true)` wrapping the model loop, bounded to 1), SessionEnd (deinit), PreCompact/PostCompact (forceCompaction), SubagentStart/Stop (spawnChildAgent), async drain at turn boundary + finalizeAll at end. is_test seam: `hooks_test_override` flag opens the gate for the integration test only. Footgun: tmpDirPath needs the path to pre-exist (realPathFile) -- build dep dirs with path.join(root,...) instead.
2026-05-29 | P3 permission-mode wire (PRD #534) | pages/gotchas/repl-runtime-permission-mode-wire.md (new), index.md | Connected the Shift+Tab permission-mode cycle to AgentRuntime. The REPL has no *AgentRuntime; it reaches the runtime only through the opaque Handler.command channel. Added `__set_permission_mode <name>` (mirrors `__yolo_on`): sets permission_mode_override AND runs transitionPermissionMode (plan-entry strips dangerous Bash allow rules). No mid-overlay re-decide because the runtime is blocked inside handler.call; cycling makes the NEXT decision use the new mode. Documented the boundary + the deferred re-decide.
- 2026-05-30 | Phase 6 MCP live-wiring task | pages/decisions/mcp-scoped-config-live-wiring.md (new) | wired structured scoped .mcp.json config + dynamic headers into the live mcp/client.zig (lazy cached ServerConfig merge, transient http_extra_headers); +2 tests; build 2337/0.
- 2026-05-30 | Phase 11 Task 11.8 (sessions-08) implementation | created pages/gotchas/session-jsonl-rewrite-and-consistency.md, updated index.md | store.removeTurnByUuid + checkResumeConsistency + SessionSnapshot.message_count_at_snapshot; count-based consistency, atomic JSONL rewrite, encrypted round-trip; Store.init inferred-error-set test gotcha
- 2026-05-30 | Phase 17 Task 17.1 (plugins-01 per-plugin enable/disable) | pages/gotchas/plugin-scope-collision-in-tests.md (new), index.md; global zig-0.16-file-write-and-keyword-footguns.md (updated) | New core/plugin_settings.zig persists `enabledPlugins` map per scope (user <zcode_home>/plugin_settings.json, workspace <cwd>/.zcode/plugin_settings.json); plugins.zig PluginSpec gains plugin_id + enabled now = effectiveEnabled(settings map, trust_default); workspace beats user; CLI enable/disable/disable-all verbs + --scope. 0.16 footguns hit: std.json.ObjectMap is unmanaged now (.empty + put(allocator,..)), Dir.rename is 5-arg use renameAbsolute. Build clean, 3367 tests pass.
- 2026-05-31 | Phase 19 Task lsp-02 (persistent LSP server manager singleton) | pages/gotchas/io-mutex-not-thread-mutex.md (updated), index.md | New core/lsp/{protocol,server_instance,manager}.zig: Content-Length framing helpers, one persistent ServerInstance per server (open stdin + background reader thread + mutex-guarded inbox keyed by request id + id-matched sendRequest), Manager singleton (ext->server map, lazy ensureServerStarted reuse, sendRequest routing, shutdown, isConnected, transitional get/install/installForTest accessor). Default ext table inlined (lsp-07 will move to config.zig). 0.16 footguns: std.Io.Condition has NO timedWait so the inbox uses a clock.sleepNanos poll loop with a deadline; std.ArrayListUnmanaged(T){} fails -> use `= .empty`. Stub server is a python3 LSP-echo written to a tmp dir, tests skip under CI. Build clean, 3461 tests pass.
- 2026-05-31 | Phase 21 Task J (sdk-headless-13 keep_alive / update_environment_variables) | pages/gotchas/env-override-map.md (new), index.md (updated) | core/env.zig gains a process-global override map (StringHashMapUnmanaged, Io.Mutex-guarded, rt.gpa-owned) consulted by getenv before libc; setOverride/getOverride/clearOverrides. sdk/structured_io.zig applyEnvUpdates parses the `variables` object (per controlSchemas.ts:629 the field is `variables` not `vars`), sets each string entry, logs key NAMES only (never values), tolerates malformed/non-object input; EnvUpdateHandler.onUpdateEnv adapts it to the on_update_env dispatch hook; keep_alive is a no-op. Transport hooks (on_keep_alive/on_update_env) already existed from Task C; this task implemented the handler + the override map. Build clean, 3629 tests pass.
- 2026-05-31 | Phase 24 Task 24.8 (ui-dialogs-09 FuzzyPicker preview parity, PARTIAL) | pages/decisions/fuzzy-picker-preview-thresholds.md (new), index.md (updated) | Extracted a pure `previewOnRight(kind, cols)` helper (QuickOpen 120 / GlobalSearch 140 / History 100) and rewired QuickOpen+GlobalSearch overlay-rows/wide decisions through it -- the only real numeric delta was GlobalSearch 120->140. QuickOpen+GlobalSearch file previews now syntax-highlight via new `langFromPath` (exact extension table, NOT prefix-based parseCodeLang which mis-maps "json"->javascript) + repl_markdown.writeCodeLine. Highlight colour is isatty-gated so it is a manual check, not unit-tested. History stayed single-column (lists prompts, no file preview to place) -- documented deviation. 4 new tests in repl_overlay.zig. Build clean, full suite 3789 passed / 2 skipped / 0 failed.
- 2026-05-31 | Phase 24 Task 24.7 (ui-dialogs-13 ClaudeMdExternalIncludesDialog, CONDITIONAL) | pages/decisions/external-claudemd-includes-refused.md (new), index.md (updated) | Verification-first task. Confirmed resolveImportPath (instructions.zig:1179) refuses every external @include (absolute paths, any `..` segment via containsParentSegment checked first, `~/` outside `.claude`/`.zcode`), so the reference's external-includes approval dialog has no class of include to gate. Downgraded to a documented deviation (zcode is stricter: refuses rather than prompts). No UI built. Shipped one pin test in instructions.zig asserting the four denial cases + relative containment + the sanctioned `~/.claude` config-dir exception. Build clean, full suite 3785 passed / 2 skipped / 0 failed.
